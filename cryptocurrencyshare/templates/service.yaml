apiVersion: v1
kind: Service
metadata:
  name: {{ include "cryptocurrencyshare.fullname" . }}
  labels:
    {{- include "cryptocurrencyshare.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cryptocurrencyshare.selectorLabels" . | nindent 4 }}
