apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edge-app-filebrowser.fullname" . }}-test-connection"
  labels:
    {{- include "edge-app-filebrowser.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edge-app-filebrowser.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
