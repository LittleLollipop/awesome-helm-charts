apiVersion: v1
kind: Service
metadata:
  name: {{ include "lp_main.fullname" . }}
  labels:
    {{- include "lp_main.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lp_main.selectorLabels" . | nindent 4 }}
